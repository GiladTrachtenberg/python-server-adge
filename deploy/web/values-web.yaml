component: web
replicas: 1

image:
  repository: video-demo-web
  tag: ""

migrations:
  enabled: false

health:
  liveness: /
  readiness: /
  port: 80

service:
  port: 80
  targetPort: 80

env:
  API_UPSTREAM: video-demo-api:8000
